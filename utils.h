#ifndef UTILS
#define UTILS

#include <stdlib.h>
#include <time.h>
#include "matmul.h"

//TODO: Description
int rand_arr(float *ptr, size_t length); 

int rand_mat(matrix *ptr, size_t width);

#endif
